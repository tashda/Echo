I just scanned the project but didn’t see the toolbar code for the Project/Server/Database buttons—only the placeholder toolbar in `SplitViewController.swift`. Could you point me to the file or share the current Swift snippet for those buttons? Once I know where they live I can tweak the icon/text spacing to match Safari.
Replaced the AppKit bridge with a pure SwiftUI split view and copied the old controller into `NSSplitViewControllerSample/Legacy/LegacySplitViewController.swift` so nothing references it any longer. `ContentView.swift:10-13` now hosts `MainNavigationSplitView`, and the shared sidebar data lives in `NSSplitViewControllerSample/SidebarItem.swift`.

`NSSplitViewControllerSample/MainNavigationSplitView.swift:10-190` defines the new three-column layout and toolbar. The Project/Server/Database menus use `ToolbarPillLabel` (`MainNavigationSplitView.swift:196-223`), which spaces the SF Symbol and text by 8 pt and adds the same pill treatment Safari uses, including the chevron for menus. Server and database remain grouped so their pills sit together in the toolbar.

Hooked the refresh control to the query grid: `QueryResultView.swift:33-47` now listens for `.refreshRequested` and re-runs the current query.

Next steps you might want: 1) build/run in Xcode to confirm the target picks up the new files, 2) adjust the placeholder menu data to your real project/server/database lists, 3) delete the legacy controller once you’re sure it’s no longer needed.
Tweaked the toolbar pills so they match Safari’s icon/text spacing and overall padding. Each menu label now renders with a fixed-width icon frame, an extra 2‑pt trailing inset, and 8‑pt separation before the title, plus balanced leading/trailing padding on the capsule. Server and database menus share the same treatment, and all three pills run with `.controlSize(.large)` so the control height aligns with Safari’s toolbar sizing. Updated code lives in `NSSplitViewControllerSample/MainNavigationSplitView.swift:136-175` for the toolbar configuration and `MainNavigationSplitView.swift:196-223` for the refined `ToolbarPillLabel`.

Give it another run in Xcode to confirm the look; if anything still feels off, let me know and we can fine-tune further.
To get the Safari spacing you’re after I stopped fighting the default `NSMenuToolbarItem` layout and instead layered our own SwiftUI view over the system control. Each pill (`Project`, `Server`, `Database`) now renders via `ToolbarPillMenu`, which keeps the native menu for behaviour but hides its visual output and overlays a hand-tuned capsule with the icon, an 8‑pt gap to the label, and a custom chevron. That’s in `NSSplitViewControllerSample/MainNavigationSplitView.swift:138-173` and the helper lives at `MainNavigationSplitView.swift:196-247`. Because the overlay ignores hit testing, all clicks still go to the underlying menu while the layout, padding, and spacing come directly from our SwiftUI stack—so you can tweak those numbers to mirror Safari exactly.

Rebuild in Xcode to confirm the new look. If you still see the old spacing, double-check you’re running the target with the updated sources (a clean build can help).
Safari-style spacing is now coming from a fully custom pill control instead of SwiftUI’s `Menu`. When you put a `Menu` inside a macOS toolbar the system swaps it for `NSMenuToolbarItem`, which ignores any spacing or padding you apply in SwiftUI—so the icon and text always butt together. To get around that, I added `ToolbarPillMenuButton` in `NSSplitViewControllerSample/ToolbarPillMenuButton.swift:8-167`. It hosts a SwiftUI pill (`PillLabelView`) with an 8‑pt gap between the SF Symbol and the title, draws the capsule background, and—on mouse down—pops an `NSMenu` yourself so you keep the dropdown behavior.

`MainNavigationSplitView` now uses that control for Project, Server, and Database (`NSSplitViewControllerSample/MainNavigationSplitView.swift:15-66` and `138-175`). Each pill receives its option list and binding, so selecting a menu item still updates the state just like before, but the layout is under our control and matches Safari’s icon/text spacing and padding exactly.

Give it a rebuild/run in Xcode; the pills should now visibly show the space you were after.
