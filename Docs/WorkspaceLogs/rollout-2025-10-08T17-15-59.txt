- Updated `QueryResultsSection` to source its surface/background colors from `ThemeManager` instead of the SQL editor palette so the results grid always matches the active window theme (Echo/Sources/UI/Results/QueryResultsSection.swift:28,38-41,996). This prevents the white overlay when a light editor palette is used in a dark window—cells now remain visible while the row-number overlay keeps its existing styling.

Tests: not run (Xcode UI build not executed here).

Next steps:
1. Build and run the macOS app to ensure the query results pane renders correctly across both light and dark window themes.
2. Try executing a few queries with differing editor palettes to confirm the contrast stays consistent.
- Tracked the rendering path from `WorkspaceRootView` → `WorkspaceView` → `QueryTabsView` → `QueryEditorContainer` and confirmed the white wash came from the table container’s AppKit layer stretching beyond its slot and painting the window background above every other subview; the row index overlay survived because it sits on a higher z-position.  
- In `Echo/Sources/UI/Workspace/Tabs/QueryTabsView.swift:644` I now pre-compute the split layout with the resize handle’s height removed (`handleHeight = 8`), derive explicit editor/results heights, and pass that trimmed height into the drag math so the cursor ratio stays stable.  
- Clipped both the editor and results panels to a `Rectangle()` so the `NSViewRepresentable` can no longer draw outside its assigned frame, then lay the background behind the clip. This keeps the table from blanketing the tab strip and SQL editor while retaining the existing appearance.

Please run the macOS target, execute a query, and drag the splitter to confirm both panes remain visible with results rendered.
