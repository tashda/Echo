Tables in the explorer still use the SF Symbol `tablecells`, so the custom `table.3x3.outlined` icon isn’t in place. You can see the current mapping in `Echo/Sources/UI/Sidebar/Components/DatabaseObjectBrowserView.swift:221`, where the row’s `iconName` switch maps:
- table → `tablecells`
- view → `eye`
- materialized view → `eye.fill`
- function → `function`
- trigger → `bolt`

If you want the custom table icon restored, the quickest follow-up is to swap the `"tablecells"` case for `"table.3x3.outlined"` in that same switch.
Custom symbols were missing because we were still calling `Image(systemName:)`, which only resolves built-in SF Symbols. I switched the row icon code to prefer symbols bundled in the asset catalog and fall back to SF Symbols only if the asset can’t be found (`Echo/Sources/UI/Sidebar/Components/DatabaseObjectBrowserView.swift:221`, `Echo/Sources/UI/Sidebar/Components/DatabaseObjectBrowserView.swift:258`). That enables the table/view/materialized-view/trigger assets (`table.3x3.outlined`, `eye.view`, etc.) to render as intended across platforms while remaining safe if an asset is ever removed.

Next step: build or run the app to confirm the custom glyphs now show up in the explorer.
Icons in the explorer now pick up each connection’s theme color instead of the hard-coded blue tint. The row view grabs `connection.color` for its tint (`Echo/Sources/UI/Sidebar/Components/DatabaseObjectBrowserView.swift:227`) and applies it to both the object icon (with template rendering so the asset can be tinted) and the trigger’s table button highlight (`Echo/Sources/UI/Sidebar/Components/DatabaseObjectBrowserView.swift:268`, `Echo/Sources/UI/Sidebar/Components/DatabaseObjectBrowserView.swift:324`).

Give the app a quick build/run to verify the custom symbols reflect the expected palette.
Explorer icons were still blue because we always tinted them with the connection color. I updated the tint logic to respect the “use server color as accent” preference: the icons (and their highlight background) now use `connection.color` only when that toggle is on, otherwise they track the app’s accent color (`Echo/Sources/UI/Sidebar/Components/DatabaseObjectBrowserView.swift:227`, `Echo/Sources/UI/Sidebar/Components/DatabaseObjectBrowserView.swift:341`). That keeps the table/view/etc. glyphs synced with the active theme choice.

Give the app a quick run to confirm the sidebar now reflects the expected accent color.
