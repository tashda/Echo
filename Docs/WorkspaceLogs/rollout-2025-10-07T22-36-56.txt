- `NavigationSplitView` would simplify the three-column story, but we would lose the granular control now living in `WorkspaceSplitViewController` ( `Echo/Sources/Platform/macOS/Controllers/WorkspaceSplitViewController.swift:53` onward )—that class enforces sidebar/inspector minimums, animates collapsing, and feeds toolbar-width updates. Re-creating that behaviour with the SwiftUI splitter is possible, yet you’re limited to its presets and column visibility API, so features like the inspector auto-collapse on small widths would need fresh workarounds.
- The TopBar is still constrained by the toolbar pipeline: `TopBarNavigator` expects an explicit width (`Echo/Sources/Presentation/Views/TopBarNavigator.swift:3`) and the `NavigatorToolbarItem` in the window controller pushes that value down from AppKit (`Echo/Sources/Platform/macOS/Windows/WorkspaceWindowController.swift:312`–`405`). Unless you move the TopBar out of the toolbar entirely, a pure SwiftUI scene doesn’t remove this coupling—you’d still need some geometry hand-off from the window/toolbar layer.
- Center content depends on AppKit glue for the tab strip and workspace (`Echo/Sources/Platform/macOS/Controllers/MainContentViewController.swift:25`–`178`). Migrating to SwiftUI means rebuilding those layout rules (dynamic tab height, background syncing, hosting controller lifecycle) and re-testing all the Combine-driven refresh triggers that currently live in the controller.
- What you gain is consistency: the sidebar, TopBar, and tab strip already exist as SwiftUI views inside hosting controllers. A SwiftUI-first scene would make styling tweaks easier and might eventually let the TopBar use native toolbar placements, but the transition is a sizeable architectural rewrite with several open questions about toolbar sizing, inspector behaviour, and macOS-version support.

If you want to pursue it, I’d prototype a separate SwiftUI window that recreates just the split view + tab bar stack, measure how well `NavigationSplitView` handles your width constraints, and only then plan a phased migration.
Replaced the AppKit workspace stack with a SwiftUI-first flow: `AppCoordinator` hands off window management to a simple `WorkspaceWindow` scene, keeps the status-bar menu working, and exposes a closure for reopening windows (`Echo/Sources/Application/AppCoordinator.swift:13`). `EchoApp` now boots into `WorkspaceWindow`’s `WindowGroup`, wiring the shared coordinator into the SwiftUI hierarchy and keeping the existing command menus intact (`Echo/Sources/Application/EchoApp.swift:11`). Reintroduced a SwiftUI `ContentView` built on `NavigationSplitView`, wiring the TopBar, tab strip, inspector, and connection sheets back together (`Echo/Sources/Presentation/Views/ContentView.swift:18`). Updated the toolbar accessory to talk directly to the coordinator instead of the deleted AppKit window subclass (`Echo/Sources/Presentation/Views/TabBar/NativeTabBarAccessories.swift:12`). Removed the unused AppKit controllers and windows that previously hosted the split view, so the workspace is driven entirely by SwiftUI (`Echo/Sources/Platform/macOS/Controllers/`…, `Echo/Sources/Platform/macOS/Windows/`…). 

Validation: `xcodebuild -scheme Echo -configuration Debug -sdk macosx build`.

Next steps: 1) run the app to confirm the status-bar shortcuts and Manage Connections sheet behave as expected in the new scene-based layout; 2) revisit any AppKit-only features you still need (e.g. custom window chrome) now that the main UI is SwiftUI.
- Gave the tab strip its Safari polish again: added spring tuning, side insets, and dynamic width math so the selected tab stretches while the rest compress smoothly (see `Echo/Sources/Presentation/Views/TabbedQueryView.swift:250` and `Echo/Sources/Presentation/Views/TabbedQueryView.swift:328`).
- Rebuilt each tab pill so the label sits dead-center, the glyph is gone, and the chrome has the taller Safari proportions ( `Echo/Sources/Presentation/Views/TabbedQueryView.swift:877` ).
- Restyled the trailing controls with the liquid-glass buttons you signed off on earlier ( `Echo/Sources/Presentation/Views/TabbedQueryView.swift:1222` ).
- Brought back keyboard navigation in the tab overview: arrow keys move the highlight, Return selects, Delete closes, and the focused card stays visually distinct ( `Echo/Sources/Presentation/Views/TabbedQueryView.swift:1503` and `Echo/Sources/Presentation/Views/TabbedQueryView.swift:1990` ).
- Kept the tab reorder fix so drops to the right land exactly where you release ( `Echo/Sources/Domain/Tabs/TabManager.swift:124` ).

Validation: `xcodebuild -scheme Echo -configuration Debug -sdk macosx build`

If you want to tweak the feel, adjust `preferredSelectedTabWidth` or `minimumCompressedTabWidth` in `TabbedQueryView.swift:251`.
- Updated the separator logic so that, while you drag a tab, only the live drop-gap divider hides; all other separators— including the ones around the tab’s temporary neighbors—stay visible the whole time (`Echo/Sources/Presentation/Views/TabbedQueryView.swift:120`).  
- Added bounds checks to the helper that picks the drop-gap index so it never points outside the valid separator range, even when you drag to either end of the strip (`Echo/Sources/Presentation/Views/TabbedQueryView.swift:123`).  

Validation: `xcodebuild -scheme Echo -configuration Debug -sdk macosx build`.
The helper that builds the tab HStack now uses `@ViewBuilder`, so Swift no longer complains about missing return inference (`Echo/Sources/Presentation/Views/TabbedQueryView.swift:329`).
I tightened the separator rendering so they no longer “fade back in” after a tab move. The gap logic still hides only the live drop slot, but I disabled animations on those separator views whenever the drag state changes, so the dividers update instantly as the tab slides past them (`Echo/Sources/Presentation/Views/TabbedQueryView.swift:340`). Build still succeeds with `xcodebuild -scheme Echo -configuration Debug -sdk macosx build`.
- Tightened the tab-strip animation and chrome: the reorder spring now snaps quicker, the strip padding drops to 6 pt with a 34 pt bar height, tab pills sit at 26 pt tall, and the trailing controls use 26 pt “glass” buttons so the layout mirrors Safari’s proportions (`Echo/Sources/Presentation/Views/TabbedQueryView.swift:250`, `Echo/Sources/Presentation/Views/TabbedQueryView.swift:877`, `Echo/Sources/Presentation/Views/TabbedQueryView.swift:1222`).  
- Removed animation from separators during drags so dividers stay rendered the moment a tab crosses them, and still only hide for the live drop gap (`Echo/Sources/Presentation/Views/TabbedQueryView.swift:339`).  
- Added full keyboard navigation to Tab Overview: the grid tracks its column count, arrow keys move focus left/right/up/down, Return reopens, Delete closes, and the focused card gets its own highlight (`Echo/Sources/Presentation/Views/TabbedQueryView.swift:1503`, `Echo/Sources/Presentation/Views/TabbedQueryView.swift:1708`, `Echo/Sources/Presentation/Views/TabbedQueryView.swift:1984`).  

Validation: `xcodebuild -scheme Echo -configuration Debug -sdk macosx build`.
- Taught the separator logic to hide any divider adjacent to the selected tab while a drag is in progress, so you never see a bar hugging the active tab.  
- Adjusted the drop-gap calculation so it skips over the selected tab entirely and clamps to the first legal slot, which keeps the in-flight divider rendered between the new neighbors while you’re still holding the dragged tab.  
- Disabled animations on those separator updates so they appear immediately as you cross each slot.

All changes live in `Echo/Sources/Presentation/Views/TabbedQueryView.swift`, and `xcodebuild -scheme Echo -configuration Debug -sdk macosx build` passes.
- Nudged the tab strip down by padding the whole `TabbedQueryView` stack so the tab bar sits a touch lower in the window, similar to Safari (`Echo/Sources/Presentation/Views/TabbedQueryView.swift:268`).  
- Reintroduced macOS 15’s native glass buttons for the “+” and overview controls, with the legacy gradient fallback kept for earlier macOS versions (`Echo/Sources/Presentation/Views/TabbedQueryView.swift:1308`).  
- Slimmed the tab chrome to match Safari: tighter top/bottom padding, 26 pt tab height, and a shorter base plate (`Echo/Sources/Presentation/Views/TabbedQueryView.swift:297`, `Echo/Sources/Presentation/Views/TabbedQueryView.swift:894`, `Echo/Sources/Presentation/Views/TabbedQueryView.swift:2314`).  
- Reworked separator logic so, while dragging, the live gap only disappears when it’s the actual drop slot and never shows next to the selected tab; separators also update immediately with animations disabled (`Echo/Sources/Presentation/Views/TabbedQueryView.swift:340`).  
- Gave the Tab Overview real grid navigation: the grid now tracks its column count, arrow keys move horizontally or vertically, Return reopens, Delete closes, and focus is maintained as tabs come and go — with visual feedback on the focused card (`Echo/Sources/Presentation/Views/TabbedQueryView.swift:1503`, `Echo/Sources/Presentation/Views/TabbedQueryView.swift:1724`, `Echo/Sources/Presentation/Views/TabbedQueryView.swift:1868`).  

Validation: `xcodebuild -scheme Echo -configuration Debug -sdk macosx build`.
Bumped the Safari base plate back up so it’s taller than the tabs—this keeps the hover highlight and shadow entirely inside the background, including in dark mode (`Echo/Sources/Presentation/Views/TabbedQueryView.swift:2321`). `xcodebuild -scheme Echo -configuration Debug -sdk macosx build` still passes.
- Nudged the tab strip down (`TabbedQueryView.swift:268`) and clipped the tabs within a rounded rect, so hover highlights now stay inside the Safari base on the left/right edges (`TabbedQueryView.swift:279`).
- Retuned the trailing controls: added breathing room between the “+” and overview buttons, nudged them closer to the strip, and used the macOS 15 `GlassButtonStyle` with active tinting while keeping the legacy gradient fallback for older systems (`TabbedQueryView.swift:1222`, `TabbedQueryView.swift:1308`).
- Reworked Tab Overview so it’s a true grid again, with adaptive column count driven by available width, geometry updates to recompute columns, arrow-key navigation (including vertical movement), and Return/Delete/Tab shortcuts for open/close/select (`TabbedQueryView.swift:1584`, `TabbedQueryView.swift:1614`, `TabbedQueryView.swift:1648`, `TabbedQueryView.swift:1674`, `TabbedQueryView.swift:1712`).
- Cleaned up the sidebar constants while doing the build fixes, ensuring the new inset constant is used consistently (`ExplorerSidebarView.swift:42`, `ExplorerSidebarView.swift:901`).

Build: `xcodebuild -scheme Echo -configuration Debug -sdk macosx build`.
- Added a dedicated horizontal inset for the tab strip so the first/last tab (and their shadows) are clipped inside the rounded Safari bar (`TabbedQueryView.swift:253`, `TabbedQueryView.swift:278`).  
- Brought spacing/treatment of the toolbar buttons back in line with the tabs: more breathing room, custom gradient highlight when active, and no more always-on accent tint (`TabbedQueryView.swift:1228`, `TabbedQueryView.swift:1306`).  
- Reworked the tab overview so it’s a true adaptive grid again, key-navigation friendly (left/right/up/down, Enter, Tab, Delete) with column counts recalculated from the available width (`TabbedQueryView.swift:1584`, `TabbedQueryView.swift:1616`, `TabbedQueryView.swift:1670`).  

Build: `xcodebuild -scheme Echo -configuration Debug -sdk macosx build`.
- Widened the Safari base plate and added horizontal insets so first/last tabs are padded within the glass; increased the toolbar strip height to 38 and removed the old clipping so the bottom glow is no longer cropped (`TabbedQueryView.swift:255`, `TabbedQueryView.swift:280`, `TabbedQueryView.swift:309`, `TabbedQueryView.swift:2314`).  
- Updated width calculations to respect the new insets, and aligned the trailing controls closer to the tabs with an 8 pt spacing (`TabbedQueryView.swift:265`, `TabbedQueryView.swift:288`).  
- Restyled the “+” and tab-overview buttons with the same soft gradients used for tabs and lighter hover/active states, dropping the accent tint for a cohesive chrome (`TabbedQueryView.swift:1350`).  
- Tab overview remains a grid with adaptive columns and keyboard navigation, and the macOS button gradient was refreshed to match the tab fill.

Build: `xcodebuild -scheme Echo -configuration Debug -sdk macosx build`.
