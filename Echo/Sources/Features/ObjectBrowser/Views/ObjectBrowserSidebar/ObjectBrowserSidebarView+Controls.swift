import SwiftUI

// Footer controls (search bar, schema picker, add button) removed.
// Search is now handled by the Search Sidebar (Cmd+Shift+F).
// Schema filtering removed — objects display schema-qualified names in the tree.
// Creation templates remain available via context menus on object type folders.
